
  # Duplicate Design (Copy)

  This is a code bundle for Duplicate Design (Copy). The original project is available at https://www.figma.com/design/TaMpYGPuNyyX1qtbCu8gyk/Duplicate-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  